[package]
name = "monad-node-monitor"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "monad-monitoring"
path = "src/main.rs"

[dependencies]
reqwest = { version = "0.12", features = ["blocking", "json"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dotenvy = "0.15"
